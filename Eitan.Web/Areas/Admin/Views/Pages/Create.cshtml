@model Eitan.Models.Project

@{
    ViewBag.Title = "הוספה";
}

<h2>הוספת עמוד תוכן</h2>

@using (Html.BeginForm("Create", "Pages", FormMethod.Post, new { enctype = "multipart/form-data" })){
    @Html.ValidationSummary(true)
        
        @Html.Partial("_SEOInput", new Eitan.Models.SEO())
        @*Html.Partial("_FBPostMessage")*@
		@Html.Partial("_CreateOrEdit", Model)
        <p>
            <input type="submit" value="הוספה" />
        </p>
}

<div>
    @Html.ActionLink("בחזרה לעמודי תוכן", "Index")
</div>


